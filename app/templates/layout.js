export const metadata = {
  title: "Templates",
  description:
    "Documents templates that you can use to make your document from base to point.",
};

export default function TemplateLayout({ children }) {
  return <section>{children}</section>;
}
